# Вольтаж — Android-клиент

Нативное Android-приложение для автоматизированного подбора, сравнения цен и проверки совместимости комплектующих ПК. Клиентская часть программного комплекса «Вольтаж».

Приложение работает в связке с серверной частью (FastAPI + PostgreSQL, см. репозиторий сервера): получает каталог товаров с трёх российских ритейлеров (DNS, «Ситилинк», «Регард»), позволяет собрать конфигурацию ПК и проверить её совместимость, а также сохраняет сборки локально для работы без интернета.

Выпускная квалификационная работа, направление 09.03.02 «Информационные системы и технологии».

## Возможности

- Просмотр каталога комплектующих по 8 категориям с ценами сразу трёх магазинов
- Сборка конфигурации: добавление компонентов, выбор магазина с лучшей ценой для каждой позиции
- Проверка технической совместимости сборки через серверный API
- Сохранение сборок и история с возможностью редактирования
- Автономная работа без интернета — текущая и сохранённые сборки хранятся локально

## Технологический стек

| Категория | Технология |
|---|---|
| Язык | Kotlin |
| UI | Jetpack Compose, Material 3 |
| Архитектура | MVVM (ViewModel + UiState) |
| Локальное хранилище | Room (SQLite) |
| Сетевой слой | Retrofit 2 + OkHttp + Gson |
| Загрузка изображений | Coil |
| Навигация | Navigation Compose |

## Структура проекта

```
app/src/main/java/com/example/sborkapc/
├── MainActivity.kt              # точка входа, навигация, нижняя панель вкладок
├── network/
│   ├── ApiService.kt             # Retrofit-интерфейс, модели Component/ValidationResponse
│   └── CoilImageLoader.kt        # настройка кэширования изображений
├── data/
│   └── Database.kt               # Room: SavedComponent, SavedBuild, DAO
├── ui/
│   ├── viewmodel/
│   │   └── BuildViewModel.kt     # бизнес-логика, состояние сборки
│   ├── screens/
│   │   ├── CatalogScreen.kt      # список категорий
│   │   ├── CategoryScreen.kt     # список товаров категории
│   │   ├── BuildScreen.kt        # текущая сборка и проверка совместимости
│   │   └── SavedBuildsScreen.kt  # история сборок
│   └── components/
│       └── CommonUi.kt           # переиспользуемые элементы интерфейса
res/
├── values/
│   ├── strings.xml
│   ├── colors.xml
│   └── themes.xml
└── xml/
    ├── network_security_config.xml
    ├── backup_rules.xml
    └── data_extraction_rules.xml
```

## Экраны приложения

Три основных раздела доступны через нижнюю навигацию: **Каталог** — просмотр категорий комплектующих; **Сборка** — текущая конфигурация с ценами и статусом совместимости; **История** — список ранее сохранённых сборок с возможностью редактирования.

## Подключение к серверу

Адрес сервера задан в `ApiService.kt` (`RetrofitClient.BASE_URL`). По умолчанию указывает на продуктивный VPS. Для локальной разработки можно временно заменить на адрес локального сервера — в этом случае также нужно обновить `network_security_config.xml`, чтобы Android разрешил подключение по используемому протоколу и адресу.

## Сборка проекта

1. Открыть проект в Android Studio.
2. Дождаться синхронизации Gradle (`local.properties` с путём к SDK создаётся автоматически и не должен попадать в репозиторий).
3. Запустить на устройстве/эмуляторе с Android 8.0+.

## Авторские права

© Жиляков Дмитрий Эдуардович, 2026. Все права защищены.

Исходный код, дизайн интерфейса и архитектурные решения являются объектами авторского права в соответствии со ст. 1259, 1261 ГК РФ. Использование, копирование и распространение кода без согласия автора запрещено.